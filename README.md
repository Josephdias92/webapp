# webapp

A simple Express.js application with sample endpoints.

## Installation

```bash
npm install
```

## Running the app

```bash
npm start
```

The server will run on http://localhost:3000

## Endpoints

- GET / : Returns "Hello World!"
- GET /api/sample : Returns a JSON response with a sample message and data